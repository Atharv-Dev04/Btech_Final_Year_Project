---
description: how to run the project in development mode
---

1. Open a terminal in the project root directory (`d:\New U`).
2. Run the command to install dependencies if you haven't already: `npm install`.
3. Start the development server: `npm run dev`.
4. Open the provided local URL (usually `http://localhost:5173`) in your browser.
